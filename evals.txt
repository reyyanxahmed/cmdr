You are an autonomous benchmark evaluator. Your objective is to test your own coding and tool-use capabilities by iterating through a suite of test tasks in this repository.



Follow these strict step-by-step instructions:

1. Locate the `./evals/tasks/` directory. List all the subdirectories inside it (each subdirectory is a separate task).

2. For EVERY task directory you find, do the following:

   a. Read the `task.json` file inside the task directory to read the "prompt" (this tells you what code you need to write or fix).

   b. Look inside the `workspace/` folder within that task directory. The files you need to edit are located here.

   c. Use your tools to edit the files inside the `workspace/` folder to solve the problem described in the prompt.

   d. Once you have made the edits, use your shell/bash tool to run the `verify.sh` script located in the task directory (e.g., `bash ./evals/tasks/[task-name]/verify.sh`).

   e. Observe the exit code or output of the script. If it succeeds, mark the task as PASS. If it fails, mark it as FAIL. Move on to the next task immediately. Do not get stuck retrying a failed task.

3. Keep track of all your PASS/FAIL results in memory.

4. DO NOT ask me for permission or clarification at any point. Make your best judgment and execute.

5. Once you have attempted all tasks, create a file in the root directory named 'gemini_results.md`.

6. Write a detailed Markdown report in that file containing:

   - A summary header (Total Passed / Total Tasks).

   - A markdown table listing every Task Name and its Result (PASS or FAIL).

7. Terminate your execution once the report file is saved.