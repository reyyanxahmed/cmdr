/**
 * LLM Adapter interface — re-exported from types for convenience.
 */

export type {
  LLMAdapter,
  LLMChatOptions,
  LLMStreamOptions,
  LLMMessage,
  LLMResponse,
  StreamEvent,
  LLMToolDef,
  TokenUsage,
} from '../core/types.js'
